<!-- Static navbar -->
<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <span class="navbar-brand">AssetManager</span>
  <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="navbar-collapse collapse" id="navbarCollapse" style="">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= is_active(devices_path) %>">
        <%= link_to 'Hardware', devices_path, :class => "nav-link" %>
      </li>
      <li class="nav-item <%= is_active(licences_path) %>">
        <%= link_to 'Software', licences_path, :class => "nav-link" %>
      </li>
      <li class="nav-item <%= is_active(people_path) %>">
        <%= link_to 'People', people_path, :class => "nav-link" %>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item <%= is_active(import_path) %>">
          <%= link_to 'Import Data', import_path, :class => "nav-link" %>
        </li>
      <% end %>
    </ul>

    <ul class="nav navbar-nav ml-auto">
      <% if user_signed_in? %>
      <li class="dropdown nav-item">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= current_user.email %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%= link_to 'Profile', edit_user_registration_path, :class => "dropdown-item" %>
          <%= link_to 'Log out', destroy_user_session_path, method: :delete, :class => "dropdown-item" %>
        </div>
      </li>
      <% else %>
      <li class="nav-item <%= is_active(new_user_session_path) %>"><%= link_to 'Log In', new_user_session_path, :class => "nav-link" %></li>
      <% end %>
    </ul>
  </div>
</nav>